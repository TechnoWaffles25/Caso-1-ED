//? Alejandro Montero Acevedo

//! METODOS
    //*getTop5();
        // Display(Int position) y que repita 5 veces
    //*getAll();
        // Display(Int position) y que repita hasta terminar la lista de noticias.
    //*Buscar(string filtro):
        // getNext()
    //*Ocultar(string filtro)
        // getNext()
        // Eliminar()
    //*Reubicar(noticia, posición):
        // obtenerPosicion(pDato):
        // Lenght(pDato):
        // Agregar()
        // Eliminar()